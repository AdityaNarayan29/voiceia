# Required:
# - Chat (LLM): llama-3.3-70b-versatile via Groq
# - Speech-to-text fallback: whisper-large-v3 via Groq
GROQ_API_KEY=

# Optional: local Kokoro TTS used for Settings voice previews.
# Main chat uses the browser's built-in speechSynthesis (free, instant).
KOKORO_API_URL=http://localhost:8880

NEXT_PUBLIC_APP_VERSION=0.1.0
